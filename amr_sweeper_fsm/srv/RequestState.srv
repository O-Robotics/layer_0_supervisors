###############################################################################
# RequestState.srv
#
# Purpose:
#   Request a transition to a target FSM-state and a specific profile.
#
# Notes:
#   - target_profile_id must be valid for the target_state's profile band.
#   - The *00 profile is the default for each FSM-state (e.g. 000, 100, 200...).
###############################################################################

# ------------------------------ REQUEST ---------------------------------------

# Target FSM-state name 
# ("INITIALIZING", "IDLING", "RUNNING", "CHARGING", "FAULT")
string target_state

# Optional lifecycle target for the *target state node*.
#   - empty / "Active"   => configure + activate (normal behavior)
#   - "Inactive"         => configure only, leaving the target node INACTIVE
string target_lifecycle

# Desired profile ID for the target_state.
uint16 target_profile_id

# Request metadata
string requester
uint8 priority
bool force
string reason

# Exact mission execution directory prepared by the scheduler for RUNNING.
# Example: /.../src/missions/<order_id>_<timestamp>/<execution_timestamp>
string mission_execution_directory

---

# ------------------------------ RESPONSE --------------------------------------

# True if the supervisor accepted the request.
bool accepted

# Current supervisor selection/state
string current_state
uint16 current_profile_id

# Desired supervisor selection/state (after accepting request)
string desired_state
uint16 desired_profile_id

# Informational message describing the result.
string message
